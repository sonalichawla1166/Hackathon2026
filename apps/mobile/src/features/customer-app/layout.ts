/** The page column beside the sidebar never runs wider than this. */
export const DESKTOP_CONTENT_MAX_WIDTH = 1100;

/** Gutter around the page column, and the gap between stacked cards. */
export const DESKTOP_GUTTER = 24;
export const DESKTOP_COLUMN_GAP = 18;
